/** @jsx React.DOM */
var React = require("react");

module.exports = React.createClass({
  displayName: "Masthead",
  render: function(){
    //@todo, intended to show what th current search was, how many results, select all
    //work with selection... etc...
    return <div>masthead</div>;
  }
});